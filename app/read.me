app/
├── main.py
├── controllers/      # request handling (optional)
├── routes/           # API endpoints (FastAPI routers)
├── services/         # business logic
├── models/           # DB models (SQLAlchemy)
├── schemas/          # Pydantic models (IMPORTANT)
├── ml/               # ML models (letter recognition, scoring)
├── utils/            # helpers (auth, hashing, tokens)
├── dataset/          # ignored in git (training data)
├── tests/            # pytest
└── config.py

need to install ffmpeg on server (need to transform audio that sending from frontend to backend way that ffmpeg undertand)
